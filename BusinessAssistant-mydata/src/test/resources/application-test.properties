spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=sa
spring.datasource.schema=classpath:schema.sql
spring.datasource.data=classpath:data.sql

#importantes las 2 de arriba para crear la base de datos en memoria y cargar los datos de prueba, asegurarse que son esos datos
# y no los de la base de datos del otro archivo mysql
#spring.datasource.data=classpath:data.sql
#consultar archivo defaults-file posible autenticacion user password d algo??

spring.h2.console.settings.web-allow-others=false
spring.h2.console.settings.trace=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# memory DB
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1

# JPA configurations
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.format_sql=true

spring.sql.init.mode=always
#spring.sql.init.mode=embedded

logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type=TRACE




# Limit user searches
user.searchLimit.limitValue=10
user.searchLimit.isLimitEnabled=true
user.searchLimit.errorMessage=Has reached the limit of searches
